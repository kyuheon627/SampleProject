// 검색창에 대한 예시 JavaScript
document.querySelector('.search-form').addEventListener('submit', function(event) {
    alert("검색을 시작합니다!");
});

// 특정 카테고리로 검색하는 함수
function filterCategory(category) {
    alert(category + " 카테고리로 검색합니다!");
    // 필요한 로직을 추가할 수 있습니다.
}
